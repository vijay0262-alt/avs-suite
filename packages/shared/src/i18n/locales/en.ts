import type { Translations } from '../index';

/** English is the canonical source; re-exported for parity with other locales. */
export { en as default } from '../index';
export type { Translations };
