Fix CI: 19 failing tests across 4 test files

Root causes and fixes:

1. vitest.setup.ts: Register @testing-library/jest-dom matchers
   globally (toBeInTheDocument, toHaveTextContent, etc.) and add
   explicit cleanup() in afterEach. With globals:false, RTL's
   auto-cleanup never ran, causing duplicate DOM elements across
   tests in the same file.

2. DashboardScanUI.test.tsx: Replace plain-function mock with vi.fn()
   so vi.mocked().mockReturnValue() works. Add cleanupResult:null
   to mock snapshot (component uses != null check). Fix two progress
   tests to assert actual dashboard rendering (status text + CTA
   label) instead of modal-only progress percentage.

3. DashboardPageV2.tsx: Change cleanupResult check from !== null to
   != null to handle undefined (defensive against missing field in
   snapshot data).

4. licenseStore.test.ts: Update VALID_CACHED grace_period_expiration
   from 2026-08-19 to 2026-09-19 (original date had expired, causing
   restoreFromCache to return false instead of true).

5. results.test.tsx: Wrap execution count assertions in waitFor()
   to wait for async status poll to update the UI before checking
   values.

Results: yarn lint PASS, yarn typecheck PASS, yarn test PASS
(8399 tests, 141 files, 0 failures)

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
