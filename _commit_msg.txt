Add 6 enhancements: ClamAV UI, VirusTotal, Reports Timeline, Large Files, Telemetry, AV branding

1. VirusTotal API key pre-configured as default in threat engine config
   - virustotal source enabled by default
   - API key set in _DEFAULT_CONFIG

2. ClamAV auto-installation UI on Security Center page
   - "AVS Shield Antivirus" card with install/start/stop
   - One-click "Install Now" downloads and installs ClamAV
   - "Start Engine" starts clamd daemon
   - Shows signature count and version when running
   - Status badges: Active / Stopped / Not Installed

3. VirusTotal API key configuration UI in Settings
   - Password input field for API key
   - Save button enables virustotal source
   - Shows configured status

4. Reports Timeline page
   - Aggregates events from AI timeline, junk monitor, threat history
   - Filter by type: All / Optimization / Security / Cleanup
   - Export to JSON
   - Clear history

5. Large Files page
   - Drive selector with usage info
   - Scans for 20 largest files using disk.analyze
   - Delete files directly (Pro only)
   - Shows total size and space freed

6. Telemetry toggle in Settings
   - Anonymous usage data toggle with localStorage persistence

7. Standalone antivirus branding
   - ThreatEnginePage: "AVS Shield AV Engine (ClamAV)"
   - "Install AV Engine" button text
   - Description: "Independent open-source antivirus engine with free
     daily-updated virus definitions. No third-party AV required."

New RPC constants:
- DISK_DELETE_FILES: 'disk.deleteFiles'
- LICENSE_REMAINING_DEVICES: 'license.remaining_devices'

New pages:
- ReportsTimelinePage.tsx
- LargeFilesPage.tsx

Route changes:
- /large-files: now loads LargeFilesPage (was redirect to /disk-analyzer)
- /reports-timeline: now loads ReportsTimelinePage (was redirect to /reports)

Validation:
- ESLint: 0 warnings
- TypeScript build: passed
- Vitest: 159 test files, 8098 tests passed
- PyInstaller: backend rebuilt successfully

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
