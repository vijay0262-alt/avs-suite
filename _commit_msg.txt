Add driver manufacturer database with download links

New Phase 2 competitive feature — closes the driver database gap vs
CCleaner's Driver Updater and IObit's Driver Booster.

Backend (drivers/driver_database.py):
  - Curated database of 25+ major hardware manufacturers
  - Covers GPU (NVIDIA, AMD, Intel), Audio (Realtek, Creative, Conexant),
    Network (Broadcom, Qualcomm, MediaTek), Chipset (ASUS, ASRock,
    Gigabyte, MSI, Dell, HP, Lenovo, Acer), Storage (Samsung, WD, Seagate),
    Input (Logitech, Microsoft), and others (Synaptics, ELAN)
  - Each entry includes:
    * Official download page URL
    * Auto-detect tool URL where available (NVIDIA, AMD, Intel, Dell, HP, Lenovo)
  - Device class to category mapping (Display->Graphics, Net->Network, etc.)
  - enrich_driver_info() adds manufacturer name, download URL, and category
    to each driver entry returned by drivers.list
  - New RPCs:
    * drivers.manufacturers — list all supported manufacturers
    * drivers.downloadLinks — get download links for installed drivers,
      deduplicated by manufacturer

Frontend (DriverUpdaterPage enhancement):
  - New "Manufacturer Download Links" card appears after scan
  - Shows one row per unique manufacturer detected on the system
  - Each row shows manufacturer name, category, and device name
  - "Download" button opens manufacturer's official download page
  - "Auto-Detect" link where the manufacturer offers an auto-detection tool
  - Links open in external browser (target=_blank, rel=noopener)
  - Loading state while fetching links

Integration:
  - drivers.list now enriches each driver with manufacturer info
  - Added DRIVERS_MANUFACTURERS and DRIVERS_DOWNLOAD_LINKS to RPC_METHODS
  - Added getManufacturers() and getDownloadLinks() to driverUpdater.service

Validation: lint clean, typecheck/build pass, 8098 frontend tests pass.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
