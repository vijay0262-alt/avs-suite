Add Cloud Drive Cleaner: scan Google Drive/OneDrive/Dropbox for large, old, and duplicate files

New Phase 2 competitive feature — closes the cloud drive cleaning gap vs
CCleaner Pro and Avast Cleanup.

Backend (cloud_drive_cleaner module):
  - Detects local sync folders for Google Drive, OneDrive, and Dropbox
  - Scans for large files (>100MB), old files (>90 days), and duplicates
    (by SHA-256 content hash)
  - RPCs: cloud_drive.detect, cloud_drive.scan, cloud_drive.status,
    cloud_drive.clean (Pro-gated)
  - Safety: only deletes files within detected cloud folders
  - No OAuth required — scans local sync copies

Frontend (CloudDriveCleanerPage):
  - Detects and displays connected cloud drives
  - Scan button with progress
  - Summary card with counts and waste breakdown
  - Large files, old files, and duplicate groups sections
  - File selection with checkboxes
  - Delete selected files (Pro only, Free users see upgrade dialog)
  - Cleanup results card

Integration:
  - Added to _FEATURE_MODULES in rpc_server.py
  - Added to PyInstaller spec hiddenimports
  - Added explicit cloud_drive prefix mapping in _explicit dict
  - Added RPC_METHODS enum entries
  - Added NavItemId type
  - Added sidebar entry with CloudArrowUpIcon
  - Added i18n key for nav label
  - Added route /cloud-drive-cleaner

Validation: lint clean, typecheck/build pass, 8098 frontend tests pass.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
