Complete antivirus: enable ClamAV, bundle definitions, auto-quarantine

This makes AVS AI Shield work as a complete antivirus product —
the user clicks scan, all detection engines run, threats are
auto-quarantined, and the user can restore or permanently delete.

1. Enabled clamav source by default:
   - _DEFAULT_CONFIG: clamav: True (was False)
   - auto_quarantine: True (was False) — threats auto-deleted on scan
   - All 7 detection engines now run in every scan:
     ClamAV, Hash Blocklist, YARA, AMSI, Heuristic, Defender, VirusTotal

2. Bundled virus definitions (107.6 MB):
   - main.cvd (84.9 MB) — base virus signatures
   - daily.cvd (22.3 MB) — latest daily signatures
   - bytecode.cvd (0.3 MB) — bytecode signatures
   - Downloaded at build time via scripts/downloadClamAv.js
   - Bundled in installer via electron-builder extraResources
   - First scan works immediately — no internet needed on first run
   - freshclam auto-updates daily.cvd in background after install

3. Auto-setup enables clamav source in config:
   - _enable_clamav_in_config() called after definitions download
   - Writes clamav: True to threat engine config file
   - Ensures scans use ClamAV even if config was previously saved
     with clamav: False

4. UI setup progress indicator:
   - AntivirusSecurityPage polls setup status every 5s
   - Shows phase: "Downloading virus definitions...",
     "Setting up antivirus engine...", "Starting antivirus engine..."
   - Message: "This happens once. Future scans start instantly
     with auto-updated definitions."

5. Scan → Detect → Quarantine → Delete flow:
   - Scan runs all enabled detectors (ClamAV, YARA, hash, AMSI, etc.)
   - Detected threats are auto-quarantined (auto_quarantine: True)
   - Threat status: detected → quarantined
   - User can restore from quarantine or permanently remove
   - ClamAV categorizes: ransomware, trojans, worms, adware/PUPs,
     spyware, generic malware

Validation:
- ESLint: 0 warnings
- TypeScript build: passed
- Vitest: 159 test files, 8098 tests passed
- Backend pytest: 1207 passed, 14 skipped, 3 warnings
- PyInstaller: backend rebuilt successfully

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
