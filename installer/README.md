# installer/

NSIS / MSI installer configuration lives alongside each app in
`apps/*/build/`. This folder collects shared licence banners, EULA
templates, and post-install scripts used by every installer.
