# tests/

Root-level e2e (Playwright) specs and shared fixtures. Unit tests live
next to their subject inside `apps/*/src` and `packages/*/src`.

```
tests/
├── e2e/         Playwright specs that drive the packaged Electron app
└── fixtures/    Reusable test data
```
